#!/bin/bash

WORKSPACE=$(cat ~/.local/var/code.subtree/workspace)
subtreepath=$(clipboard.out)
realpath=$(realpath ${WORKSPACE}/${subtreepath})
rm "${WORKSPACE}/${subtreepath}"
test -f "${realpath}" && code "${realpath}"
